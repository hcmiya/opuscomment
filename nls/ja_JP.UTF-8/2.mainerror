$ 間違った使い方やプログラム全体で起こるその他エラー
$ 『opuscomment: 』と前置される

$set 2

$ -l
5 タグ出力時は-tを使用できない

$ 
6 タグ編集時は-a|-wの指定が必要

$ 
7 ゲイン調整のオプションは-lと同時に使用できない

$ 
8 ファイル指定がない

$ 
9 引数が多過ぎる

$ %1$u = TAG_LENGTH_LIMIT__OUTPUT >> 20
10 保存出来るタグの長さが上限を超えた(%uMiBまで)

$ 
11 タグ入力が長過ぎる。変なコマンドを挟んでいませんか?

$ -D で入力が空のとき
12 空のタグ入力

$ -T でファイルが'\n'で終わってないとき
13 タグ入力が改行で終わっていない

$ -d は -l|-w と排他。編集モードオプションがない時は -a が暗に指定される
14 タグ削除は -a と一緒に使うこと
